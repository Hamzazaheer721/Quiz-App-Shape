import styled from "styled-components";

export const BalanceScreenWrapper = styled.div`
  padding-inline: 2rem;
  padding-block: 0.7rem;
  font-size: 1.8rem;

  & > :not(:last-child) {
    margin-block-end: 0.8rem;
  }
`;
